import { pgTable, uuid, text, timestamp, jsonb, boolean, bigint, index } from "drizzle-orm/pg-core";
export const users=pgTable("users",{id:uuid("id").defaultRandom().primaryKey(),displayName:text("display_name").notNull(),status:text("status").notNull().default("active"),createdAt:timestamp("created_at",{withTimezone:true}).defaultNow().notNull()});
export const signingRequests=pgTable("signing_requests",{id:uuid("id").defaultRandom().primaryKey(),state:text("state").notNull(),repository:text("repository").notNull(),ref:text("ref").notNull(),commit:text("commit").notNull(),buildId:text("build_id").notNull(),artifactName:text("artifact_name").notNull(),artifactSha256:text("artifact_sha256").notNull(),artifactSize:bigint("artifact_size",{mode:"number"}).notNull(),profile:text("profile").notNull(),context:jsonb("context").notNull().default({}),createdAt:timestamp("created_at",{withTimezone:true}).defaultNow().notNull(),updatedAt:timestamp("updated_at",{withTimezone:true}).defaultNow().notNull()},t=>[index("signing_requests_state_idx").on(t.state),index("signing_requests_sha_idx").on(t.artifactSha256)]);
export const approvals=pgTable("approvals",{id:uuid("id").defaultRandom().primaryKey(),requestId:uuid("request_id").notNull(),actorId:uuid("actor_id").notNull(),decision:text("decision").notNull(),bindingHash:text("binding_hash").notNull(),assurance:text("assurance").notNull(),createdAt:timestamp("created_at",{withTimezone:true}).defaultNow().notNull()});
export const hsmDevices=pgTable("hsm_devices",{id:uuid("id").defaultRandom().primaryKey(),serial:text("serial").notNull().unique(),role:text("role").notNull(),label:text("label"),metadata:jsonb("metadata").notNull().default({}),enabled:boolean("enabled").notNull().default(true)});
export const auditEvents=pgTable("audit_events",{id:uuid("id").defaultRandom().primaryKey(),occurredAt:timestamp("occurred_at",{withTimezone:true}).defaultNow().notNull(),actor:text("actor").notNull(),action:text("action").notNull(),target:text("target"),outcome:text("outcome").notNull(),requestId:uuid("request_id"),payload:jsonb("payload").notNull().default({}),previousHash:text("previous_hash"),eventHash:text("event_hash").notNull().unique()},t=>[index("audit_time_idx").on(t.occurredAt)]);
export const pushSubscriptions=pgTable("push_subscriptions",{id:uuid("id").defaultRandom().primaryKey(),userId:uuid("user_id").notNull(),endpoint:text("endpoint").notNull(),keyData:jsonb("key_data").notNull(),createdAt:timestamp("created_at",{withTimezone:true}).defaultNow().notNull()});
