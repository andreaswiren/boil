# Security regression suite

Add tests for authorization bypass, replay, CSRF, SSRF, audit redaction, setup-token reuse, push approval replay, signing state races, policy confusion, filename/path traversal, network rollback, service allowlist bypass, PKCS#11 mechanism abuse and HSM device swapping.
