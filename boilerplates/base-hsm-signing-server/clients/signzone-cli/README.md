# SignZone CLI

Planned Rust CLI for request/status/download/verify operations against the REST API. Authentication should use workload OIDC, device/passkey flow, or mTLS rather than stored administrator passwords.
