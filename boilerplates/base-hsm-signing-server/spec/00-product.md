# Product Specification

SignZone is a dedicated signing appliance. Users monitor/request signings; approvers authorize policy-gated requests; administrators manage identity, policies, HSMs, certificates and the host through structured controls. The default deployment is a single Debian 13 appliance with PostgreSQL and two optional Nitrokey HSM 2 devices (primary + DR).
