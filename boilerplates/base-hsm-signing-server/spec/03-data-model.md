# Data Model

Core entities: users, identities, sessions, webauthn credentials, TOTP metadata, roles, capabilities, API identities, OIDC issuers, signing profiles, policies, signing requests, approvals, artifacts, certificates, key metadata, HSM devices, DKEK ceremonies, wrapped backups, audit events, push subscriptions, system config revisions. See `src/db/schema.ts`.
